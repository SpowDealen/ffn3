import type {ReactElement, ReactNode} from "react";
export default function EditorialWorkspaceScreen({children}: {children: ReactNode}): ReactElement { return <div className="laboratory-editorial-workspace">{children}</div>; }
