import type {RetrievalResult} from "../types";
export default function RetrievalDiagnostics({result}: {result: RetrievalResult}) { return <details><summary>Diagnóstico técnico</summary><dl className="review-definition-grid"><dt>Scoring</dt><dd>{result.scoringVersion}</dd><dt>Query fingerprint</dt><dd>{result.query.queryFingerprint}</dd><dt>Fecha</dt><dd>{new Date(result.createdAt).toLocaleString("es-ES")}</dd><dt>Stale</dt><dd>{result.stale ? `Sí: ${result.staleReasons.join(" ")}` : "No"}</dd></dl>{result.limitations.length ? <ul>{result.limitations.map((item) => <li key={item}>{item}</li>)}</ul> : null}</details>; }
