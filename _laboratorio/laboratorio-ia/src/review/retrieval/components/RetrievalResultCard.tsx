import type {RetrievalCandidate} from "../types";
import RetrievalExplanation from "./RetrievalExplanation";
export default function RetrievalResultCard({candidate}: {candidate: RetrievalCandidate}) { return <article className={`retrieval-card retrieval-${candidate.positiveOrNegative}`}><header><div><h6>{candidate.evidenceType}</h6><p>{candidate.memoryId}</p></div><span className="review-badge">{candidate.candidateStatus} · {candidate.finalScore}/100</span></header><div className="retrieval-metrics"><span>Compatibilidad <strong>{candidate.compatibilityStatus}</strong></span><span>Confianza histórica <strong>{candidate.confidenceScore}/100</strong></span><span>Clúster <strong>{candidate.clusterState}</strong></span><span>Policy <strong>{candidate.reusePolicy}</strong></span></div><RetrievalExplanation value={candidate.explanation} /><details><summary>Provenance y fingerprints</summary><pre>{JSON.stringify(candidate.provenance, null, 2)}</pre></details></article>; }
