import type {RetrievalExplanationModel} from "../types";
export default function RetrievalExplanation({value}: {value: RetrievalExplanationModel}) { return <div className="retrieval-explanation"><p>{value.outcome}</p><p>{value.compatibility}</p>{value.matches.length ? <ul>{value.matches.map((item) => <li key={item}>{item}</li>)}</ul> : null}{[...value.differences, ...value.missing, ...value.blocks, ...value.warnings].length ? <details><summary>Diferencias, ausencias y bloqueos</summary><ul>{[...value.differences, ...value.missing, ...value.blocks, ...value.warnings].map((item, index) => <li key={`${item}:${index}`}>{item}</li>)}</ul></details> : null}</div>; }
