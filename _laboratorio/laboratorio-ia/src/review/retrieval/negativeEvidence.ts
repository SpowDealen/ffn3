import type {RetrievalCandidate} from "./types";
export const evaluateNegativeEvidence = (candidate: RetrievalCandidate) => ({isNegative: candidate.positiveOrNegative === "negative", strength: candidate.compatibilityStatus === "exact" ? "exact" : candidate.compatibilityStatus === "incompatible" ? "incompatible" : "contextual", message: candidate.compatibilityStatus === "exact" ? "Una decisión equivalente fue rechazada editorialmente; se presenta como bloqueo contextual, no como regla universal." : "Una decisión cercana fue rechazada editorialmente; se presenta como advertencia histórica."});
