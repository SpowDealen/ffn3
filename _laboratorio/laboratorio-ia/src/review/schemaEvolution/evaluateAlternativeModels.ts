import type {SchemaConstraintDiagnosis, SchemaImpactAssessment, SchemaModelAlternative} from "./types";
const alternative = (diagnosis: SchemaConstraintDiagnosis, impacts: SchemaImpactAssessment[], type: SchemaModelAlternative["type"], title: string, description: string, score: number, benefits: string[], risks: string[], migration = false): SchemaModelAlternative => ({id: `${diagnosis.entityType}:${diagnosis.field}:${diagnosis.outcome}:${diagnosis.recommendation.type}:${type}`, type, title, description, score, benefits, risks, requiresMigration: migration, affectedAreas: impacts.filter((item) => item.affected).map((item) => item.area)});
export function evaluateAlternativeModels(diagnosis: SchemaConstraintDiagnosis, impacts: SchemaImpactAssessment[]): SchemaModelAlternative[] { const alternatives = [alternative(diagnosis, impacts, "keep_current", "Mantener el modelo actual", "Conservar la restricción y documentar que los casos no representables seguirán bloqueados.", 28, ["Sin migración inmediata."], ["Mantiene el incentivo para inventar datos."]), alternative(diagnosis, impacts, "make_optional", "Convertir la relación en opcional", "Permitir ausencia cuando la realidad editorial no contenga esa relación.", diagnosis.recommendation.type === "make_optional" ? 84 : 72, ["Representa entidades legítimas incompletas."], ["Builders y consultas deben tolerar ausencia."], true), alternative(diagnosis, impacts, "split_relationship", "Dividir los significados de la relación", "Modelar por separado cada rol editorial hoy mezclado en el mismo campo.", diagnosis.recommendation.type === "split_relationship" ? 92 : 76, ["Evita asociaciones falsas.", "Hace explícita la semántica de cada relación."], ["Requiere auditar consultas, productores y documentos existentes."], true), alternative(diagnosis, impacts, "new_document", "Introducir un documento de relación", "Representar roles, vigencia y procedencia en un documento relacional explícito.", 66, ["Admite historial y evidencia por relación."], ["Mayor complejidad operativa y de consulta."], true)]; return alternatives.sort((left, right) => right.score - left.score || left.id.localeCompare(right.id)); }
