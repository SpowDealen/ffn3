import {isSerializableReviewValue} from "../cases/validateResolution";
import type {SchemaEvolutionProposal} from "./types";
const unique = (values: string[]): boolean => new Set(values).size === values.length;
export function validateSchemaProposal(proposal: SchemaEvolutionProposal): {valid: boolean; errors: string[]} { const errors: string[] = []; const expectedIdentity = `${proposal.entityType}:${proposal.field}:${proposal.outcome}:${proposal.recommendationType}`; if (!isSerializableReviewValue(proposal)) errors.push("La propuesta no es JSON serializable."); if (proposal.identity !== expectedIdentity || proposal.id !== `proposal:${expectedIdentity}`) errors.push("La identidad estructural de la propuesta no es estable."); if (!proposal.sourceIssueIds.length || !unique(proposal.sourceIssueIds)) errors.push("sourceIssueIds debe ser no vacío y no contener duplicados."); if (!proposal.affectedEntityKeys.length || !unique(proposal.affectedEntityKeys)) errors.push("affectedEntityKeys debe ser no vacío y no contener duplicados."); if (proposal.occurrenceCount !== proposal.sourceIssueIds.length) errors.push("occurrenceCount no coincide con las incidencias de origen."); if (!proposal.problem.trim()) errors.push("Falta el problema editorial."); if (!proposal.alternatives.length) errors.push("Faltan modelos alternativos."); if (!unique(proposal.alternatives.map((item) => item.id))) errors.push("Existen alternativas duplicadas."); if (![proposal.facts, proposal.violatedRules, proposal.contradictions, proposal.benefits, proposal.warnings].every(unique)) errors.push("Existen textos duplicados en la propuesta."); if (!unique(proposal.evidence.map((item) => item.id))) errors.push("Existen evidencias duplicadas."); if (!unique(proposal.impacts.map((item) => item.area))) errors.push("Existen áreas de impacto duplicadas."); if (proposal.alternatives.some((item) => !unique(item.benefits) || !unique(item.risks))) errors.push("Existen beneficios o riesgos duplicados."); if (proposal.alternatives.some((item) => item.score < 0 || item.score > 100)) errors.push("Existe una puntuación fuera de 0..100."); if (proposal.confidence < 0 || proposal.confidence > 1) errors.push("La confianza está fuera de 0..1."); return {valid: errors.length === 0, errors}; }
